interface:
  display_name: "AgentOSNext PR Merge Review"
  short_description: "Independent exact-head AgentOSNext PR review"
  default_prompt: "Use $agentosnext-pr-merge-review to independently decide APPROVE or REQUEST_CHANGES for an AgentOSNext Forgejo PR at its exact current head."

policy:
  allow_implicit_invocation: true
